{
  "name": "@muggleai/plugins",
  "version": "0.0.0",
  "description": "Plugin contracts and lifecycle primitives for muggle-ai-works",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo \"plugins build delegated to root\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo \"plugins lint delegated to root\"",
    "test": "echo \"plugins test delegated to root\""
  }
}
