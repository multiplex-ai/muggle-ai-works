# Bootstrap Tooling

This folder is reserved for one-command setup scripts and validation utilities.

Planned scripts:

- workspace bootstrap (install + build)
- environment doctor checks
- local machine setup helpers
