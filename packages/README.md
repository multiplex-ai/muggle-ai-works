# Packages Workspace

This folder hosts reusable runtime packages consumed by `apps/*` entrypoints.

Current package domains:

- `mcps`
- `commands`
- `plugins`
- `skills`
- `workflows`
- `agents`
- `hooks`
